# Скелет черновика статьи (Этап 6)

> **Метод (Этап 6 методологии).** Один длинный заход записи в `work/<slug>/draft.md`: служебные поля (frontmatter) сверху + тело. Публикация и точный формат вёрстки - забота адаптера `cms.adapter` на Этапе 9; здесь задача написать чистый переносимый текст в GitHub-flavored Markdown, без компонентов конкретного движка.
>
> **Как использовать этот файл.** Скопируй всё, что ниже разделителя `==== НАЧАЛО ЧЕРНОВИКА ====`, в `work/<slug>/draft.md` и заполни плейсхолдеры. Блоки в `<...>` - заглушки под содержимое; квадратные пометки `[блок ...]` - места, где `cms.adapter` подставит ваш визуальный компонент при публикации.
>
> **Дефис, не длинное тире.** В заголовке, meta-полях, теле, цитатах, источниках - везде дефис `-`. Длинное тире - маркер ИИ-текста, под запретом по всей методологии.

---

==== НАЧАЛО ЧЕРНОВИКА ====

```yaml
---
slug: <slug>
title: "<заголовок-лидер из Этапа 3>"
title_variants:          # 2 запасных заголовка из Этапа 3 - для A/B через cms.adapter
  - "<вариант 2>"
  - "<вариант 3>"
type: <обзорная / инструкция / разбор-кейс / рецепт>
difficulty: <для новичка / средний / продвинутый>
target_keyword: "<ключевой запрос>"
hero_promise:            # обещание читателю - рендерится в начале статьи, в теле не дублировать
  what_you_get:
    - <что читатель получит, буллет 1>
    - <буллет 2>
    - <буллет 3-5>
  apply_in: <за сколько минут применит>
  saves: <сколько времени экономит>
meta_title: "<до 60 символов>"
meta_description: "<до 155 символов>"
excerpt: "<1-2 предложения - анонс статьи>"
cta_offer: "<ВАШ_ОФФЕР из config.yaml>"
cta_url: "<cta.url из config.yaml>"
status: draft
# --- опциональные поля (по контексту темы) ---
# parent_slug: <slug>          # если статья в кластере вокруг опорной
# prerequisites: [<slug>]      # если читать только после другой статьи
# tools:                       # если статья про конкретный инструмент
#   - name: "<название>"
#     url: "<ссылка>"
---
```

<!-- Обещание читателю НЕ писать руками в теле: рендерится из hero_promise. -->

## CTA-ТОЧКА 1 - подписка / первый контакт

`<короткая проза-связка: «оставайся на связи по теме <ниша>», без агрессивной продажи>`

[блок подписки - рендерится из config.yaml]

<!-- Дальше идёт тело. Первая смысловая строка - TL;DR или H2, не служебный лог. -->

## `<H2 раздел 1 - что это и зачем>`

> Коротко: `<TL;DR, 1-2 предложения прямого ответа - первым блоком в разделе>`

`<основной текст раздела, абзацы по 2-4 предложения>`

## `<H2 раздел 2 - в форме вопроса>`

> Коротко: `<TL;DR>`

`<текст>`

> «`<дословная цитата>`»
> - Имя автора, название источника, `<ссылка>`

<!-- Цитата только с атрибуцией: текст + автор + ссылка. Без атрибуции не ставить. -->

## `<H2 раздел 3 - в форме вопроса>`

> Коротко: `<TL;DR>`

`<текст>`

## CTA-ТОЧКА 2 - встроенный оффер (на 25-35% длины, после 2-3 разделов)

`<связка темы с оффером: «<тема> - это <роль темы в вашем оффере>; <ВАШ_ОФФЕР> даёт полную картину», без давления, без цен/дат руками>`

[блок оффера, вариант «встроенный» - рендерится из config.yaml]

## `<H2 раздел 4 - как сделать, ядро; уникальная фактура>`

> Коротко: `<TL;DR>`

`<текст>`

Готовый промпт / шаблон / скрипт - в fenced-блоке с языком на первой строке (читатель копирует одним кликом):

```text
<готовый текст промпта / шаблона без шаблонных подстановок движка>
```

Код и команды - тоже fenced-блоком:

```bash
<команда>
```

Шаги - нумерованным списком:

1. `<шаг 1: заголовок + текст>`
2. `<шаг 2>`
3. `<шаг 3>`

Предупреждение / совет - выноской с явной пометкой типа:

> **Важно.** `<текст предупреждения - адаптер превратит в цветную врезку>`

## `<H2 раздел 5 - в форме вопроса>`

> Коротко: `<TL;DR>`

Сравнение - таблицей, не сплошным текстом:

| `<критерий>` | `<вариант A>` | `<вариант B>` |
|---|---|---|
| `<...>` | `<...>` | `<...>` |

## `<H2 раздел N - грабли и крайние случаи>`

> Коротко: `<TL;DR>`

`<текст>`

<!-- Добавляй H2-разделы до 8-14, каждый с TL;DR первым блоком. -->

## Источники

> - `<первоисточник 1 - название, ссылка>`
> - `<первоисточник 2>`
> - `<...минимум 5 именованных ссылок из Этапа 4...>`

## CTA-ТОЧКА 3 - подвал (короткий мостик)

`<1-2 предложения: «полная система по теме <ниша> - за один заход; <ВАШ_ОФФЕР>: коротко, по делу, с результатом, который остаётся у вас»>`

[крупный блок оффера в подвале - вставляет cms.adapter; если адаптер не умеет авто-подвал, поставь блок оффера руками один раз здесь]

==== КОНЕЦ ЧЕРНОВИКА ====

---

## Памятка по телу (Этап 6, перед сохранением)

- [ ] TL;DR первым блоком в КАЖДОМ H2 (1-2 предложения)
- [ ] обещание читателя НЕ продублировано руками (рендерится из `hero_promise`)
- [ ] все цитаты с атрибуцией (текст + автор + ссылка)
- [ ] промпты / шаблоны / код - в fenced-блоках с языком
- [ ] первая смысловая строка тела - TL;DR или H2, НЕТ строк-логов инструментов вне fenced-блоков
- [ ] минимум 3 внутренние ссылки на `content_path` (только на реальные страницы)
- [ ] блок «Источники» в конце, минимум 5 именованных ссылок
- [ ] 3 CTA-точки на местах, без давящих формулировок и без цен/дат руками
- [ ] минимум 1 таблица, процессы - нумерованными списками
- [ ] везде дефис `-`, ни одного длинного тире
