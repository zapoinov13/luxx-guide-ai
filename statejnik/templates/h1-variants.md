# Заголовок статьи - 3 варианта (Этап 3)

> **Метод (Этап 3 методологии).** Заголовок пишется для человека, который за 1 секунду должен понять, **что он сделает, прочитав статью, и зачем это ему**. Если тот же заголовок совпадает с тем, как человек гуглит - это и есть хороший SEO. Обратное неверно: «совпадает с ключевым запросом» != «понятно человеку».
>
> Заполняй сверху вниз: сначала интент, потом классификация термина, потом формула, потом 3 варианта. Не пропускай шаги.

---

## Поля статьи

| Поле | Значение |
|---|---|
| Рабочая тема | `<тема из Этапа 2>` |
| `target_keyword` (ключевой запрос) | `<...>` |
| `slug` (lowercase, kebab-case, без не-ASCII) | `<...>` |
| `type` (тип статьи) | `<обзорная / инструкция / разбор-кейс / рецепт>` |
| `difficulty` (уровень) | `<для новичка / средний / продвинутый>` |
| Аудитория (`audience.profile`) | `<кто читатель - из config.yaml>` |
| `audience.jargon_level` | `<low / high>` |

---

## Шаг 3.1 - Интент (минимум 3 формулировки)

> Что человек **делает**? Глагол + объект на человеческом языке. Технический термин - это не интент.

1. `<глагол + объект, формулировка 1>`
2. `<глагол + объект, формулировка 2>`
3. `<глагол + объект, формулировка 3>`

Самая популярная формулировка интента (по ней пойдёт топ-1 заголовок): `<...>`

---

## Шаг 3.2 - Классификация термина

> Тест: «гуглил бы это слово человек, который только столкнулся с темой?» Да - якорь. Нет - жаргон. При сомнении (особенно при `jargon_level: low`) - жаргон.

| Термин | Якорь или жаргон | Обоснование |
|---|---|---|
| `<термин>` | `<якорь / жаргон>` | `<гуглят по имени / узнают на странице>` |

---

## Шаг 3.3 - Формула заголовка по типу термина

- **Якорь:** `<Термин>: <утилитарный хвост>`
- **Жаргон:** `Как <глагол> <объект> через <термин>: <как именно>`

Главная проверка для жаргонной темы: прикрой пальцем всё после «через». Осталось понятно человеку без термина - валидно. Без термина теряет смысл - переписывай.

Выбранная формула: `<...>`

---

## Шаг 3.4 - Три варианта (human-first / search-first)

> Требования: вариант 1 - без термина в первых 30 символах (действие в начале); вариант 2 - с термином рядом с ключевым словом; все 3 проходят human-test (звучат как живая фраза из разговора); длина 50-70 символов.

### Вариант 1 - human-first (без термина в первых 30 символах) → топ-1 по умолчанию

```
<заголовок 1>
```

- Символов: `<N>` (целевое 50-70)
- Первые 30 символов без термина: `<да / нет>`
- Human-test (звучит как живая фраза): `<да / нет>`
- Совпадает с интентом №: `<1 / 2 / 3>`

### Вариант 2 - search-first (термин рядом с ключом) → в `title_variants` для A/B

```
<заголовок 2>
```

- Символов: `<N>`
- Термин рядом с ключевым словом: `<да / нет>`
- Human-test: `<да / нет>`

### Вариант 3 - запасной → в `title_variants` для A/B

```
<заголовок 3>
```

- Символов: `<N>`
- Human-test: `<да / нет>`

---

## Решение (автономно)

- **Топ-1 (в `title`):** `<вариант, ближайший к самой популярной формулировке интента из 3.1>`
- **В `title_variants` для A/B:** `<2 оставшихся варианта>`

> Анти-маркеры, которые валят human-test (если вылезли - переписать): «настройка и автоматизация», «пошаговая инструкция и шаблон», «6 шагов и воркфлоу», парные хвосты «X и Y» в конце. Это почерк ИИ-копирайтера, не живого автора.
